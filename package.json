{
  "name": "listening",
  "version": "1.0.0",
  "private": true,
  "description": "Listening — 跨平台音乐搜索与播放器",
  "scripts": {
    "build": "npx esbuild src/api/index.js --bundle --format=iife --global-name=ListeningAPI --outfile=examples/api-bundle.js",
    "start": "node server.mjs",
    "dev": "node server.mjs"
  }
}
